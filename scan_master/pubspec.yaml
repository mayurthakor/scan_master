name: scan_master
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Existing dependencies
  cupertino_icons: ^1.0.8
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  image_picker: ^1.1.2
  firebase_storage: ^12.4.7
  cloud_firestore: ^5.6.9
  cloud_functions: ^5.5.2
  url_launcher: ^6.3.1
  webview_flutter: ^4.13.0
  razorpay_flutter: ^1.4.0
  file_picker: ^10.2.0
  intl: ^0.20.2
  provider: ^6.1.5
  share_plus: ^11.0.0
  
  # NEW: Camera and document scanning dependencies
  camera: ^0.10.6
  path_provider: ^2.1.5
  image: ^4.0.17
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true